name: Random GitHub Activity

on:
  workflow_dispatch:
  schedule:
    - cron: "0 1 * * *"

jobs:
  random-activity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Create random activity
        run: |
          echo "$(date): Random activity from GitHub Actions" >> activity.log
          git config user.email "alirezanaseri548@users.noreply.github.com"
          git config user.name "alirezanaseri548"
          git add activity.log
          git commit -m "Random update from GitHub Actions"
          git push
